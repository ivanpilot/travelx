<%= errors_messages(@board)%>

<%= display_create_activity_form_from_board(@activity) %>
<br>

<%= display_create_board_form(@board)%>
<br>

<h3>Boards</h3>
<ul>
  <% @boards.each do |board| %>
    <li><%= link_to board.title, board_path(board) %></li>
  <% end %>
</ul>
