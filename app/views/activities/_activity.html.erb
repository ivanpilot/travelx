<%= activity.label :description %><br>
<%= activity.text_area :description, autocomplete:"off" %><br>
<%= activity.label :rating %><br>
<%= activity.text_field :rating, autocomplete:"off" %><br><br>
<h3>Belongs to boards</h3>
<%= activity.collection_check_boxes :board_ids, @boards, :id, :title do |b|
    b.label(class: 'checkbox') {b.check_box + b.text}
end %><br>
